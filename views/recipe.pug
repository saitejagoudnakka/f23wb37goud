extends layout

block content
    h1= title
    -
        var Recipe  = [{"type": "Indian", "name": "Dosa", "price": 50},{"type": "Italian", "name": "Pasta", "price": 200},{"type": "chinese", "name": "noodles", "price": 150}]

    div(class="container-fluid")
        div(class="row")
            each food, index in Recipe
                div(class="col-sm-2" onclick =`buy(${index})`) #{food.type}, #{food.name}, #{food.price}Rs       
                script.
                 function eat(a) {
                   console.log(alert(`You enjoyed the ${tastyFoods[a].taste} food!`))}



                    
                    